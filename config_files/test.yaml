# Config file for PointSDF test evaluation
# Produces the same metrics as pointcraft (CD mm, F-score, volume MAE/RMSE/R2)
# Run: python scripts/test.py

folder_sdf: '19_03_224023'        # Run folder under results/runs_sdf (set to your trained run)
root_dir: 'data'  # relative to PointSDF project root (contains 1_rgbd, 2_sfm, 3_pair)

# Mesh extraction
resolution: 20         # Grid resolution — matches corepp's grid_density: 20 (8k points, single decoder batch)

# Metric computation
num_points_sample: 10000   # Points sampled from predicted and GT meshes for CD / F-score

# Inference mode
run_refinement: false      # If true, run iterative latent refinement after encoder forward pass
max_inference_epochs: 300  # Only used when run_refinement: true
lr: 0.00001
lr_scheduler: true
lr_multiplier: 0.5
patience: 50
sigma_regulariser: 0.01
clamp: true
clamp_value: 0.1
